/*
 * @author  Lars Hendahl
 */

 define([ "lib/elocalstoragelib" ], function(ls) {
 	describe("elocalstoragelib", function() {
 		it("expect(ls).toBeDefined()", function() {
 			expect(ls).toBeDefined();
 		});
 	});
 });